services:
  - type: web
    name: financial-intelligence-os
    env: python
    buildCommand: buildCommand: pip install --upgrade pip wheel setuptools && pip install --prefer-binary -r requirements.txt
    startCommand: uvicorn apps.main:app --host 0.0.0.0 --port $PORT
